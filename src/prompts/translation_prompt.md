Translate the values in the following JSON structure to {target_language}.
Keep the keys and structure exactly the same.
Only translate values that are human-readable text (names, descriptions, comments, notes).
Do not translate dates, numbers, IDs, or status codes (like 'pending', 'confirmed').

JSON:
{json_str}
